# Core scraping
requests==2.31.0
beautifulsoup4==4.12.3
lxml==5.2.1          # faster HTML parser (optional, bs4 will use it if installed)
playwright>=1.44.0   # headless browser — run: playwright install chromium
playwright-stealth>=1.0.6  # hides automation fingerprints from Cloudflare

# Environment
python-dotenv==1.0.1

# Backend
fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy==2.0.30
psycopg2-binary==2.9.9   # only needed when switching to PostgreSQL

# Utils
httpx==0.27.0        # async HTTP client (for later async scrapers)
